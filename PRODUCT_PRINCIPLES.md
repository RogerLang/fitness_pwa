# 练了么：产品原则与长期方向

这份文档记录“练了么”后续迭代时优先遵守的产品方向。它不是功能清单，也不是商业化路线图；当未来出现新想法时，先判断它是否服务于这些原则。

## 1. 核心目的

练了么首先是一个个人力量训练工具。它要做的事情是：

1. 如实记录实际发生的训练。
2. 长期保留清楚、可追溯的训练历史。
3. 在积累一段时间的数据后，为人与 ChatGPT 共同复盘训练表现提供可靠材料。
4. 提供低摩擦的接口，让 ChatGPT 可以协助升级训练模板和进步节奏。
5. 最终训练决定始终由训练者自己做；到健身房后可以根据状态、器械、时间等现实条件微调本次任务。

一句话概括：

> 练了么负责记录和执行，ChatGPT 负责阶段性分析和快速修改，训练者保留最终决定权。

## 2. 记录优先

训练历史是最重要的数据资产。

- Session 记录“实际做了什么”，不要为了让数据看起来整齐而改写历史。
- 本次训练中的临时调整可以和长期模板不同。
- 已完成训练应尽量保持不可变；后续模板变化不应反向改变历史记录。
- 热身、正式组、重量、次数、RIR、完成状态等字段应保持明确含义。

## 3. 模板、计划和记录的边界

继续保持当前单向流程：

```text
训练模板 → 当前待训练计划 → 训练记录
```

- 训练模板：描述以后通常怎么练。
- 当前待训练计划：描述下一次准备怎么练，可以临时调整。
- 训练记录：描述最终实际怎么练。

修改长期模板，不应静默改写已经推送的当前训练计划；修改本次计划，也不应自动反向修改长期模板。

## 4. ChatGPT 的角色

ChatGPT 不需要实时控制每一组训练，也不需要每天自动接管训练决策。

主要使用方式：

- 积累一段时间后，一起复盘训练表现。
- 根据历史讨论下一阶段的训练节奏。
- 协助调整动作、组数、次数区间、起始重量、重量步进等模板参数。
- 把用户用自然语言描述的修改，转换成 App 可以校验和应用的结构化变更。

例如：

> 明天练胸，把哑铃上斜卧推换成杠铃上斜卧推，其他内容先保持不变。

用户不应为了这种修改手工进入多个字段反复编辑。App 应提供一个简单接口，把当前模板交给 ChatGPT，再把经过检查的修改结果带回 App。

## 5. 用户控制权

- ChatGPT 的输出默认是建议或变更提案。
- 结构化修改在写入前必须由 App 校验。
- 涉及长期模板的修改应先展示差异，再由用户确认。
- 用户可以拒绝、部分采用，或在训练现场继续微调。
- 不因为引入 AI 而取消手工编辑能力。

## 6. 动作身份与历史连续性

`exerciseId` 表示动作在训练历史中的身份，不只是显示名称。

需要区分两种情况：

- 改名：例如修正措辞、统一名称。保留 `exerciseId`，历史连续。
- 换动作：例如“哑铃上斜卧推 → 杠铃上斜卧推”。创建新的 `exerciseId`，避免把两个动作的历史和进阶数据错误混在一起。

未来任何自动修改接口都应保留这一区别。

## 7. 自动进阶的定位

现有确定性进阶逻辑继续作为日常建议基础。短期内不为了“智能化”而让 AI 每次训练都参与。

未来如果调整进阶节奏，优先通过透明的长期模板参数表达，例如：

- 目标次数区间
- 组数
- 起始重量
- 重量步进
- 其他明确、可解释的规则

阶段性复盘可以由 ChatGPT 建议这些参数如何变化，再由用户确认。

## 8. 本地优先与隐私

继续保持 local-first：

- 本机 IndexedDB 是主要使用数据。
- Private GitHub 作为当前个人多设备同步方案。
- 未保存草稿、同步凭据等敏感信息不进入对外分析交接数据。
- 提供给 ChatGPT 的数据应按任务需要最小化，不默认导出无关信息。

## 9. 产品化与商业化

未来可以考虑封装成正式 Android / iOS App，甚至公开发行或收费，但这不是当前开发的驱动因素。

当前优先级仍然是：

> 先把它做成一个自己长期训练时真正愿意使用、记录可靠、修改省事的工具。

如果未来商业化，尽量让现有训练核心、数据语义和用户控制原则继续成立，而不是提前为了假设中的大量用户重写当前产品。

## 10. 当前迭代判断标准

增加一个功能前，优先问：

- 它是否让训练记录更准确？
- 它是否让历史更容易用于复盘？
- 它是否减少计划修改的重复劳动？
- 它是否保留用户最终决定权？
- 它是否破坏当前已经好用的 Training 体验？

如果答案不明确，可以先不做。

## v157 起的结构化模板交接

第一步只解决模板修改，不直接接入 AI API。

交接分两段：

```text
练了么导出 fitness-template-handoff-v1
        ↓
用户把 JSON + 自然语言要求发给 ChatGPT
        ↓
ChatGPT 返回 fitness-template-change-v1
        ↓
练了么校验 → 展示差异 → 用户确认 → 应用模板修改
```

支持的第一批操作：

- `renameExercise`：改名，保留动作身份。
- `updateExercise`：调整组数、次数区间、起始重量、重量步进等参数。
- `replaceExercise`：真正换动作，创建新的动作身份。
- `addExercise`：新增动作。
- `removeExercise`：删除模板动作，历史保留。
- `moveExercise`：调整动作顺序。

这个接口只修改长期模板，不自动改写已经推送的当前训练计划。
